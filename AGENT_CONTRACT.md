# 実験を実行するエージェントの契約

入力は対象実験の `protocol.json`、`cohorts.json`、`results/summary.csv`／`report.json`。実行入口は `experiment.run(seed, gain, gate)`。既定は `brain-four-pn-readout`、過去の対照には明示的な `experiment=` を使う。

- 実行前に刺激対象、時間窓、停止辺、到来時刻、seed、入力利得、模型・データのハッシュを検査する。
- 現在のハーネスは凍結された条件だけを受け付ける。未知seed・利得・gate、書き換えられた契約・計測コードは実行前に拒否する。
- 新しい条件は新しい実験フォルダへ記述し、必要なハーネス変更と検査を揃えてから実行する。過去の判定表・ハッシュ・結果を置換しない。
- `gain` は刺激頻度の倍率。回路の `cfg.gain=0.65` と混同しない。`hungry`／`satiated` は1.0／0.5のラベルで、生理学的状態の推定ではない。
- OFFは外部から来る刺激／身体センサーのON／OFF。固定試験ではprotocolの時刻表に従う。PN出力をOFF判定へ戻す循環は許さない。
- 読み出しは指定した4 PN。停止対象は全686 ALPNへの既知正符号ALLN入力。読み出しと介入の集合を入れ替えない。
- 不一致は失敗として保存・報告する。閾値や参照ハッシュを結果に合わせて更新して合格にしない。

返り値は `experiment, seed, gain, gate, protocol_sha256, metrics, replay_verification`。4 PN停止条件では対応する対照も新規実行して比を計算する。保存済み結果を返すだけのAPIではない。全記録は新規の実行ディレクトリに残る。`reference_files_byte_identical` はその実行で実際に比較できたファイル数を表す。

合否の意味：ピーク比70%以上、後半比30%以上、OFF平均3 Hz以下は操作的な参考条件。個別4細胞の0 HzとOFF1秒以後の全区間無発火は別フラグ。生体への妥当性や他刺激への一般化を返さない。
